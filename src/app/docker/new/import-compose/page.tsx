import { ComposeImport } from '@/components/Docker/ComposeImport';
import { Container } from '@mantine/core';

export default function ImportComposePage() {
  return (
    <Container fluid>
       <ComposeImport />
    </Container>
  );
}
